interface ITextField {
	placeholder: string;
	name: string;
	label: string;
};

const TextField = ({ placeholder, name, label }: ITextField) => {
	return (
		<div></div>
	);
};

export { TextField };
