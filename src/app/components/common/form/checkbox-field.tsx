import { FC } from 'react';

// ДОДЕЛАТЬ КОМПОНЕНТ

const CheckboxField: FC = ({ classesParent }) => {
	return (
		<div className={`${classesParent}__checkbox-field field-checkbox-block`}>

		</div>
	);
};

export { CheckboxField };
